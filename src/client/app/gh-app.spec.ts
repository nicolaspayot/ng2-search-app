import {describe, it, expect, beforeEachProviders, inject} from 'angular2/testing';
import {GhApp} from '../app/gh-app';

beforeEachProviders(() => [GhApp]);

describe('App: GhApp', () => {
  //TODO
});
